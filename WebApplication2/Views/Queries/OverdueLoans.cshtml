@model IEnumerable<OverdueLoansViewModel>

<h2>Overdue Loans</h2>
<table class="table">
    <thead>
        <tr>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Amount</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.FirstName</td>
                <td>@item.LastName</td>
                <td>@item.Amount</td>
            </tr>
        }
    </tbody>
</table>
